<h1>Listing qtdeambientes</h1>

<table>
  <thead>
    <tr>
      <th>Qtde</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @qtdeambientes.each do |qtdeambiente| %>
      <tr>
        <td><%= qtdeambiente.qtde %></td>
        <td><%= link_to 'Show', qtdeambiente %></td>
        <td><%= link_to 'Edit', edit_qtdeambiente_path(qtdeambiente) %></td>
        <td><%= link_to 'Destroy', qtdeambiente, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Qtdeambiente', new_qtdeambiente_path %>
